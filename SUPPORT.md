# Support

Before opening an issue, confirm that FFmpeg is installed with `ffmpeg -version`, try one short file, and check the output folder is writable.

For a conversion report, include macOS version, MP4Flow version, source container/codec information, the selected conversion mode, and the error text. Do not attach private videos unless you are comfortable sharing them.
